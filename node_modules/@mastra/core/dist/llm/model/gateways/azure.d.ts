import type { LanguageModelV2 } from '@ai-sdk/provider-v5';
import { MastraModelGateway } from './base.js';
import type { ProviderConfig } from './base.js';
export interface AzureAccessToken {
    token: string;
    expiresOnTimestamp?: number;
}
export interface AzureTokenCredential {
    getToken(scopes: string | string[], options?: unknown): Promise<AzureAccessToken | null>;
}
export interface AzureOpenAIGatewayConfig {
    resourceName: string;
    apiKey?: string;
    apiVersion?: string;
    deployments?: string[];
    authentication?: {
        type: 'entraId';
        credential: AzureTokenCredential;
        scope?: string;
    };
    management?: {
        tenantId: string;
        clientId: string;
        clientSecret: string;
        subscriptionId: string;
        resourceGroup: string;
    };
}
export declare class AzureOpenAIGateway extends MastraModelGateway {
    private config;
    readonly id = "azure-openai";
    readonly name = "azure-openai";
    private tokenCache;
    private entraIdTokenRequests;
    constructor(config: AzureOpenAIGatewayConfig);
    private validateConfig;
    fetchProviders(): Promise<Record<string, ProviderConfig>>;
    private getManagementCredentials;
    private getAzureADToken;
    private fetchDeployments;
    buildUrl(_routerId: string, _envVars?: typeof process.env): undefined;
    getApiKey(_modelId: string): Promise<string>;
    private getEntraIdToken;
    private fetchEntraIdToken;
    private createEntraIdFetch;
    resolveLanguageModel({ modelId, apiKey, headers, }: {
        modelId: string;
        providerId: string;
        apiKey: string;
        headers?: Record<string, string>;
    }): Promise<LanguageModelV2>;
}
//# sourceMappingURL=azure.d.ts.map